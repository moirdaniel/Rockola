[package]
name = "core-scan"
version = "0.1.0"
edition = "2021"

[dependencies]
walkdir = "2"
thiserror = "1"
rusqlite = { version = "0.31", features = ["bundled"] }
time = "0.3"

core-db = { path = "../db" }
core-domain = { path = "../domain" }
core-events = { path = "../events" }

